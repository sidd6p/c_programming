//enum is user defined data type
//All unassigned names get value as value of previous name plus one.
//The value assigned to enum names must be some integeral constant(signed int)
//static const, const, enum, #define ?????????????????????????????????????????????????????????????????????????????

#include<stdio.h>

enum week{Mon, Tus, Wed = 1, Thr, Fri, Sat, Sun};
//enum week_1{Mon, Tue}; error because defination of Mon is already done in enum week

int main(){
    enum week day;
    day = Thr;
    printf("%d", day);
    return 0;
}